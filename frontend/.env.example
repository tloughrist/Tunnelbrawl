# Base URL of the Tunnel Brawl Rails backend (no trailing slash).
# Used by src/config.js as `import.meta.env.VITE_API_URL`.
# Local development points at the Rails server on port 3000.
# For production builds, set this to the deployed backend host.
VITE_API_URL=http://localhost:3000
